<form
	action="<% if (folder) {%>
	/myfiles/create/<%=folder.id%><%} else {%>/myfiles/create/<%}%>"
	method="POST"
>
	<label for="folderName">Folder Name:</label
	><input type="text" name="folderName" id="folderName" />
	<button>Add Folder</button>
</form>
