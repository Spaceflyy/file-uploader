<form
	action="<% if (folder) {%>
	/upload/<%=folder.id%><%} else {%>/upload<%}%>"
	method="POST"
	enctype="multipart/form-data"
>
	<label for="file">Choose a file:</label>
	<input type="file" name="file" />
	<button>Upload</button>
</form>
